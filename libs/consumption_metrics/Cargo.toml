[package]
name = "consumption_metrics"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"

[dependencies]
chrono = { workspace = true, features = ["serde"] }
rand.workspace = true
serde.workspace = true
